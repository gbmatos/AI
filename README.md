# Incident Management Website

This is a simple Node.js application providing a basic incident management website.

## Running

Install dependencies and start the server:

```bash
npm install
npm start
```

Then open `http://localhost:3000` in your browser.
